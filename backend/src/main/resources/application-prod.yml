# 生产环境配置
spring:
  datasource:
    url: jdbc:mysql://xxxxx:xxxx/feedback_system?useSSL=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8mb4&allowPublicKeyRetrieval=true
    username: xxxx
    password: xxxxx

# MyBatis Plus 日志（生产环境关闭）
mybatis-plus:
  configuration:
    log-impl:

# 日志级别
logging:
  level:
    root: WARN
    com.feedback: INFO
